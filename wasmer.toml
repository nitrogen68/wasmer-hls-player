[package]
name = "your-wasmer-username/your-app-name"
version = "0.1.0"
description = "My static website"

[dependencies]
"wasmer/static-web-server" = "^1"

[fs]
# Change "public" to wherever your index.html lives 
# (e.g., "dist", "build", or "." if it's right in the root folder)
public = "public" 
